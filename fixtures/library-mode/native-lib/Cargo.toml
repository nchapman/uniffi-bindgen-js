[package]
name = "library-mode-fixture"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
uniffi = { version = "0.31.0", features = ["build"] }
thiserror = "1"

[workspace]
