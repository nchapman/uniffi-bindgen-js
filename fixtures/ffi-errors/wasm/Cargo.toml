[package]
name = "ffi-errors"
version = "0.1.0"
edition = "2021"

[lib]
name = "ffi_errors"
crate-type = ["cdylib"]

[dependencies]
uniffi = { version = "0.31.0", features = ["scaffolding-ffi-buffer-fns", "wasm-unstable-single-threaded"] }
thiserror = "1"

[workspace]
