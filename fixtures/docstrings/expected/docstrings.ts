// Generated by uniffi-bindgen-js. DO NOT EDIT.
import { UniffiRuntime, UniFFIWriter, UniFFIReader } from './uniffi_runtime.js';

const _rt = await UniffiRuntime.load(new URL('./docstrings.wasm', import.meta.url), 'docstrings');

/** A 2D point value. */
export interface Point {
  /** Horizontal position. */
  x: number;
  /** Vertical position. */
  y: number;
}

/**
 * The mood of the user.
 * - `Happy`: Feeling good.
 * - `Sad`: Feeling bad.
 */
export type Mood = 'Happy' | 'Sad';

/** A text processing callback. */
export interface Processor {
  /** Processes the input text and returns the result. */
  process(input: string): string;
}

// --- VTable for callback interface Processor ---
_rt.registerCallbackVTable('Processor', 'uniffi_docstrings_fn_init_callback_vtable_processor', [
  {
    params: ['i64'], results: [],
    fn: (handle: bigint) => { _rt.removeCallbackHandle(handle); },
  },
  {
    params: ['i64'], results: ['i64'],
    fn: (handle: bigint) => { return _rt.cloneCallbackHandle(handle); },
  },
  {
    params: ['i64', 'i32', 'i32', 'i32'], results: [],
    fn: (_handle: bigint, _arg0: number, _outPtr: number, _statusPtr: number) => {
      const _savedScratch = _rt.scratchSave();
      try {
        const _obj = _rt.getCallbackHandle(_handle) as any;
        const _lifted0 = (() => { const _rb = _rt._readRustBufferStruct(_arg0); return _rt._readUtf8(_rb.dataPtr, _rb.len); })();
        const _result = _obj.process(_lifted0);
        const _retRb = _rt.lowerString(_result); _rt._writeRustBufferStruct(_outPtr, _retRb);
        _rt._writeCallStatusSuccess(_statusPtr);
      } catch (_e) {
        _rt._writeCallStatusPanic(_statusPtr, _e);
      } finally {
        _rt.scratchRestore(_savedScratch);
      }
    },
  },
]);

/** A counter that can be incremented and decremented. */
export class Counter {
  /** @internal */
  readonly _handle: bigint;
  private _freed = false;
  private _assertLive(): void {
    if (this._freed) throw new Error('Counter object has been freed');
  }
  private constructor(handle: bigint) {
    this._handle = handle;
    _rt.registerPointer(this, 'uniffi_docstrings_fn_free_counter', handle);
  }
  /** @internal */
  static _fromHandle(handle: bigint): Counter { return new Counter(handle); }
  /** Creates a new counter starting at the given value. */
  static create(start: bigint): Counter {
    const _argPtr = _rt.scratchAlloc(1 * 8);
    _rt.writeI64Element(_argPtr, BigInt(start));
    const _retPtr = _rt.scratchAlloc(5 * 8);
    try {
      _rt.call('uniffi_ffibuffer_docstrings_fn_constructor_counter_new', _argPtr, _retPtr);
      _rt.checkCallStatus(_retPtr + 8);
      const _result = _rt.readHandleElement(_retPtr);
      return new Counter(_result);
    } finally {
      _rt.scratchReset();
    }
  }
  /** Returns the current value. */
  get(): bigint {
    this._assertLive();
    const _clonedHandle = _rt.cloneObjectHandle('uniffi_docstrings_fn_clone_counter', this._handle);
    const _argPtr = _rt.scratchAlloc(1 * 8);
    _rt.writeHandleElement(_argPtr, _clonedHandle);
    const _retPtr = _rt.scratchAlloc(5 * 8);
    try {
      _rt.call('uniffi_ffibuffer_docstrings_fn_method_counter_get', _argPtr, _retPtr);
      _rt.checkCallStatus(_retPtr + 8);
      const _result = _rt.readI64Element(_retPtr);
      return _result;
    } finally {
      _rt.scratchReset();
    }
  }
  /** Increments the counter by one. */
  increment(): void {
    this._assertLive();
    const _clonedHandle = _rt.cloneObjectHandle('uniffi_docstrings_fn_clone_counter', this._handle);
    const _argPtr = _rt.scratchAlloc(1 * 8);
    _rt.writeHandleElement(_argPtr, _clonedHandle);
    const _retPtr = _rt.scratchAlloc(4 * 8);
    try {
      _rt.call('uniffi_ffibuffer_docstrings_fn_method_counter_increment', _argPtr, _retPtr);
      _rt.checkCallStatus(_retPtr);
    } finally {
      _rt.scratchReset();
    }
  }
  /** Releases the underlying WASM resource. Safe to call more than once. */
  free(): void {
    if (this._freed) return;
    this._freed = true;
    _rt.unregisterPointer(this);
    _rt.callFree('uniffi_docstrings_fn_free_counter', this._handle);
  }
}
if (Symbol.dispose) (Counter as any).prototype[Symbol.dispose] = Counter.prototype.free;

// --- Serialization helpers ---

function _lowerPoint(w: UniFFIWriter, value: Point): void {
  w.writeI32(value.x);
  w.writeI32(value.y);
}
function _liftPoint(r: UniFFIReader): Point {
  return {
    x: r.readI32(),
    y: r.readI32(),
  };
}

function _lowerMood(w: UniFFIWriter, value: Mood): void {
  if (value === 'Happy') { w.writeI32(1); return; }
  if (value === 'Sad') { w.writeI32(2); return; }
  throw new Error(`Unknown Mood variant: ${value}`);
}
function _liftMood(r: UniFFIReader): Mood {
  const ordinal = r.readI32();
  if (ordinal === 1) return 'Happy';
  if (ordinal === 2) return 'Sad';
  throw new Error(`Unknown Mood ordinal: ${ordinal}`);
}

/** The docstrings demo namespace. */
export namespace Docstrings {
  /**
   * Returns a greeting for the given name.
   * Supports Unicode names.
   */
  export function greet(name: string): string {
    const _rb_name = _rt.lowerString(name);
    const _argPtr = _rt.scratchAlloc(3 * 8);
    _rt.writeRustBufferElements(_argPtr, _rb_name);
    const _retPtr = _rt.scratchAlloc(7 * 8);
    try {
      _rt.call('uniffi_ffibuffer_docstrings_fn_func_greet', _argPtr, _retPtr);
      _rt.checkCallStatus(_retPtr + 24);
      const _result = _rt.liftString(_rt.readRustBufferElements(_retPtr));
      return _result;
    } finally {
      _rt.scratchReset();
    }
  }
}
