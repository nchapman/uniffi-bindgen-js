[package]
name = "uniffi_simple_fns"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

# Stand-alone workspace so it can be compiled independently of the generator.
[workspace]
